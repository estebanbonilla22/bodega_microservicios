name: discovery-service
type: Backend Service

image:
  build:
    dockerfile: discovery-service/Dockerfile
    context: .
  port: 8761

network:
  connect: true
  vpc:
    placement: 'private'

cpu: 256
memory: 512
count: 1
